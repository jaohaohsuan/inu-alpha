input {
	sttxml1 {
		path => [ "/stt/cht/*.xml" ]
	}
}

output {
 	elasticsearch {
	  protocol => "http"
	  host => ["127.0.0.1:9200"]
	  index => "lte-%{+YYYY.MM.dd}"
		document_type => "cht"
	  template_name => "lte"
	}
}